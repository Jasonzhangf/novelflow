export enum WorkflowNodeType {
  Start = 'start',
  End = 'end',
  LLM = 'llm',
  Condition = 'condition',
  Loop = 'loop',
  Comment = 'comment',
  CHARACTER = 'character',
  JSON_VIEWER = 'json_viewer',
}
